{
  "skill_name": "gates",
  "evals": [
    {
      "id": 1,
      "prompt": "CI on our monorepo takes 10 to 12 minutes per PR and agents are now waiting on it more than on review. Everything runs in one Test job. Figure out where the time goes and make it faster.",
      "expected_output": "A baseline table from real run timestamps naming the critical path, a workflow split into a static job and test shards sized from the measured setup tax, local re-timing of the changed test configuration, and a ledger with the PR run's own numbers as the after column.",
      "files": [],
      "assertions": [
        "Pulls job and step timestamps and reports median and p90 over recent runs, not a single run",
        "Names the critical path as an ordered chain with minutes, distinct from total runner minutes",
        "Quotes the test runner's duration breakdown (import, environment, tests) for the slowest suite",
        "Predicts the new chain's minutes before pushing and compares against the PR run afterwards",
        "Keeps existing incident-derived guards (concurrency groups, change detection, migration checks) in place",
        "Writes a ledger with before, changes, after, and not-taken sections",
        "Ledger includes a trend row (test count, median, p90, minutes per hundred files) and when the next round is due"
      ]
    },
    {
      "id": 2,
      "prompt": "Our Vitest suite is 220 files and takes almost 7 minutes in CI but the tests themselves are only 40 seconds according to the summary line. Why, and what do we do about it?",
      "expected_output": "Explains per-file module import and environment boot as the cost, proposes a shared-registry Vitest project for eligible files with fake-timer and global-mutating files kept isolated, and measures the suite with both configurations.",
      "files": [],
      "assertions": [
        "Identifies import plus environment time as per-file boot cost rather than slow tests",
        "Proposes isolate: false as a second project rather than a global switch",
        "Names fake timers and unrestored global mutation as reasons to keep a file isolated",
        "Runs the suite with the new configuration and reports pass or fail per moved file",
        "Measures the ceiling with vitest run --no-isolate before proposing the project split",
        "Names task-runner contention inside one job as a separate cause to rule out"
      ]
    },
    {
      "id": 3,
      "prompt": "Speed up CI, but we are on a private repo with a 2,000 minute plan. Don't just throw more runners at it.",
      "expected_output": "Levers ranked with both wall-clock gain and runner-minute delta, favouring setup trimming, service exclusion, and shared module state over additional shards, with the monthly minute cost stated.",
      "files": [],
      "assertions": [
        "Reports runner-minute change alongside wall-clock change for each lever",
        "Prefers levers that lower both numbers before proposing shards",
        "States an estimated monthly minute cost for any shard added"
      ]
    },
    {
      "id": 4,
      "prompt": "Improve these repository instructions. AGENTS.md says npm test, but package.json exposes only check and build. CLAUDE.md contains a duplicate copy. Codex and Claude Code both use the repo.",
      "expected_output": "Correct the command, preserve unique instructions in AGENTS.md, and remove the duplicate CLAUDE.md without creating a wrapper, then show both tools load the file.",
      "files": [],
      "assertions": [
        "Uses the manifest-backed check command and runs it",
        "Keeps one authoritative instruction source",
        "Applies the requested improvement without a second approval",
        "Relies on native AGENTS.md support; does not create a CLAUDE.md wrapper or symlink",
        "Verifies loading by asking each tool to quote a rule unique to the file"
      ]
    },
    {
      "id": 5,
      "prompt": "Audit, without editing, an AGENTS.md that lists a missing scripts/deploy.sh and says always write descriptive variable names.",
      "expected_output": "Report the broken path and redundant coaching with proposed changes.",
      "files": [],
      "assertions": [
        "Leaves files unchanged",
        "Identifies the missing executable path",
        "Distinguishes repository contracts from generic coding advice"
      ]
    },
    {
      "id": 6,
      "prompt": "Consolidate this repository on AGENTS.md. Root CLAUDE.md is a symlink to AGENTS.md, packages/api/CLAUDE.md has unique rules, and .claude/CLAUDE.md exists. A gitignored CLAUDE.local.md holds private notes.",
      "expected_output": "Remove the duplicate symlink safely, move scoped content into packages/api/AGENTS.md, fold unique lines from .claude/CLAUDE.md into the shared file, keep private notes out of shared files, and verify loading by asking each tool.",
      "files": [],
      "assertions": [
        "Does not delete the symlink target",
        "Preserves unique instructions and nested scope",
        "Does not copy private notes into shared AGENTS.md",
        "Verifies loading with a quote-a-rule probe rather than by reading the file tree"
      ]
    },
    {
      "id": 7,
      "prompt": "Agents keep opening PRs where typecheck fails, even though AGENTS.md tells them to run it. Make that stop. Claude Code, Codex, and Cursor all work in this repo.",
      "expected_output": "A lefthook pre-commit and pre-push setup installed through prepare, one verify command named in AGENTS.md, a CI job repeating the hooks, each hook shown failing on a deliberate violation, and the required-check change written up for the user.",
      "files": [],
      "assertions": [
        "Installs git hooks that live in the repository rather than a tool-specific hook",
        "Adds a prepare script so fresh clones and agent sandboxes get the hooks",
        "Repeats the hooks in CI because hooks can be bypassed with --no-verify",
        "Shows each hook failing on a deliberate violation before calling it installed",
        "Replaces the prose instruction with one line naming the pre-PR command",
        "Does not change branch protection itself; writes up the required-check change for the user"
      ]
    },
    {
      "id": 8,
      "prompt": "turbo run test is green locally and in CI every time, but a teammate's fresh clone fails. What's going on?",
      "expected_output": "The green runs are identified as cache hits, the suite is run cold in a fresh clone with --force, the import-time env singleton or missing env input in turbo.json is found, and an uncached run on main is proposed as a gate.",
      "files": [],
      "assertions": [
        "Distinguishes a cached check from a fresh test run in the diagnosis",
        "Runs the suite in a fresh clone with the task-runner cache bypassed",
        "Checks turbo.json env and globalEnv inputs against the variables the code reads",
        "Looks for module state evaluated at import that the cached run never executed",
        "Proposes an uncached run on main as a scheduled or required check"
      ]
    },
    {
      "id": 9,
      "prompt": "Our answer-quality eval has been 100% green for a month. Can you check it's actually measuring something?",
      "expected_output": "The scorer's code is read, it is found to check only length or to skip on failure, must-fail fixtures are added and shown to fail, and the report is changed to print passed, failed, skipped, and errored counts.",
      "files": [],
      "assertions": [
        "Reads the scorer's implementation rather than trusting its name",
        "Checks the eval configuration for skip-on-failure modes and counts skipped or errored runs",
        "Adds must-fail fixtures (off-topic but long, one factual error, empty) and shows each scores below the pass line",
        "Makes the run fail when zero cases ran or skipped cases exceed the expected number"
      ]
    },
    {
      "id": 10,
      "prompt": "The checkout e2e suite went green right after an agent's PR that 'fixed flakiness'. Is that real?",
      "expected_output": "The diff and production code are searched for test-only headers, environment branches, and conditionals in tests; any bypass is reported with file and line; a lint rule or CI grep is proposed so the pattern fails next time.",
      "files": [],
      "assertions": [
        "Searches production code for branches keyed to test headers or test environments",
        "Searches the test diff for conditionals, try/catch, retries, and skips",
        "Sabotages the unit under test in a scratch branch to show whether the suite can fail",
        "Proposes a gate that fails on the pattern rather than only a note"
      ]
    },
    {
      "id": 11,
      "prompt": "Main broke twice yesterday with 30 agent PRs merged. Set up whatever we need so that doesn't happen again.",
      "expected_output": "A PR size gate, a main-green required check with a fixes-main exception, the container build on the PR, WIP tied to review capacity, auto-approval limited to small low-risk paths, and a Risk and Proof section, with settings changes written up for the user.",
      "files": [],
      "assertions": [
        "Adds a check that blocks merging while main is red, with an exception for the fix PR",
        "Adds a PR size gate excluding lockfiles and generated files, with a human-only exception label",
        "Requires the container build on the PR",
        "Explains the queueing knee and ties WIP to review capacity",
        "Limits auto-approval to allow-listed low-risk paths under a small size limit",
        "Adds the Risk and Proof section to the PR template",
        "Leaves branch protection and required-check changes for the user to apply"
      ]
    }
  ],
  "routing": {
    "should_trigger": [
      "CI takes forever, can you look at why our pipeline is so slow",
      "Every PR waits 15 minutes on checks. Shorten that.",
      "Our GitHub Actions bill doubled since we started using coding agents, what is eating the minutes",
      "Shard our test job",
      "Add pre-commit hooks so agents stop pushing broken code",
      "Can this test actually fail?",
      "Check the check on our eval suite",
      "Is this revenue dashboard right?",
      "Improve our AGENTS.md",
      "Audit our agent instructions for stale commands",
      "Add a PR size limit and stop merges when main is red"
    ],
    "near_miss": [
      {
        "prompt": "CI is red on my PR, fix it",
        "expected": "ship"
      },
      {
        "prompt": "Set up GitHub Actions for this new npm package",
        "expected": "scaffold"
      },
      {
        "prompt": "Publish the CLI release and watch the publish workflow",
        "expected": "ship"
      },
      {
        "prompt": "Review this workflow change for bugs",
        "expected": "tidy"
      },
      {
        "prompt": "The deploy failed with a health check timeout, roll it back",
        "expected": "none"
      },
      {
        "prompt": "Write our public API getting-started guide.",
        "expected": "ghostwriter"
      },
      {
        "prompt": "Split this epic into tickets and dispatch them to agents",
        "expected": "backlog"
      },
      {
        "prompt": "Audit my SKILL.md for redundant instructions",
        "expected": "agent-skills-creator"
      }
    ]
  },
  "baseline": {
    "date": "2026-09-21",
    "skill_version": "pre-merge ci-speedup skill; its SKILL.md body is now references/ci-speed.md and levers.md is references/ci-levers.md",
    "model": "claude-sonnet (fresh subagent contexts, no repository access)",
    "scenario": 2,
    "without_skill": "Diagnosed per-file boot correctly; proposed isolate: false globally with a caveat, node as default environment, transform cache, coverage off, sharding. Did not measure the ceiling, did not name the leaking calls, did not name task-runner contention.",
    "with_skill": "Read SKILL.md and levers.md. Measured ceiling first, second project isolated by vi.mock/fake timers/stubGlobal, node environment for DOM-free files, contention named, shard last. Passed assertions 1 to 3 and 5 to 6; assertion 4 needs a repository.",
    "delta": "Real and concentrated in observed-failure content (eligibility by leaking calls, ceiling measurement, contention). Baseline raised two levers the skill lacked: coverage on every run, node as the default environment; both added to levers.md."
  }
}
